export interface Owner {
  id?: number;
  firstName: string;
  lastName: string;
  description?: string;
}
export interface OwnerCreate {
  id?: number;
  firstName: string;
  lastName: string;
  description?: string;
}
